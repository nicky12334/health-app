# Netlify build settings — so the GitHub import needs no manual configuration.
[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for any unmatched route.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
